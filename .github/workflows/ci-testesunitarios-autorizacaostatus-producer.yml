name: CI testes unitarios autorizacaostatus-producer

on:
  push:
    paths:
      - "apps/autorizacaostatus-producer/**"
  pull_request:
    paths:
      - "apps/autorizacaostatus-producer/**"

jobs:
  testes-unitarios:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
          cache-dependency-path: apps/autorizacaostatus-producer/pom.xml

      - name: Rodar testes unitarios
        working-directory: apps/autorizacaostatus-producer
        run: mvn -B test -Dtest='!*IntegrationTest'

# modules/observability

**Status:** placeholder — sem código Terraform ainda.

## Propósito futuro

Módulo reutilizável para observabilidade dos serviços em ECS/Fargate: log
groups do CloudWatch (destino padrão de stdout dos contêineres), métricas e
alarmes básicos (saúde do serviço, uso de CPU/memória, erros do ALB).

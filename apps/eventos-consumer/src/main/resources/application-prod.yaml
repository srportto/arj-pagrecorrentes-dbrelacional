management:
    endpoint:
        health:
            show-details: never

kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    schema-registry-url: ${KAFKA_SCHEMA_REGISTRY_URL}
    topic: ${KAFKA_TOPIC:eventos-autorizacao}
    group-id: ${KAFKA_GROUP_ID:eventos-consumer}

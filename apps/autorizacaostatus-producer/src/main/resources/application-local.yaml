management:
    endpoint:
        health:
            show-details: always

spring:
    cloud:
        aws:
            endpoint: http://localhost:4566
            region:
                static: us-east-1
            credentials:
                access-key: test
                secret-key: test

sqs:
    queue-url: http://localhost:4566/000000000000/SQS-eventos-autorizacao

kafka:
    bootstrap-servers: localhost:19092
    schema-registry-url: http://localhost:8085
    topic: eventos-autorizacao

# Copie para .env (git-ignorado) e ajuste os valores antes de `docker compose up`.
# Sem DB_PASSWORD definida, a subida falha com erro explícito (nao ha mais valor padrao).

DB_NAME=db-csp-postgres
DB_USER_NAME=docker
DB_PASSWORD=<defina-sua-senha-local>
